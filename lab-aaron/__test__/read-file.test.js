'use strict';

const readIt = require('../lib/read-file');

describe('Should show first 8 hex values from txt files', function() {
  test('Should return the first 8 hex characters', done => {
    console.log(readIt.read());
    done();
  });
});

describe('Should show first 8 hex values from txt files', function() {
  test('Should return the first 8 hex characters', done => {
    console.log(readIt.read());
    done();
  });
});

describe('Should show first 8 hex values from txt files', function() {
  test('Should return the first 8 hex characters', done => {
    console.log(readIt.read());
    done();
  });
});
